This repo will track the design and improvement of a simple snake game.

The Snake GUI is developed using the NCurses library on a Linux machine or the PDCurses GUI on a Windows Machine.
    - This condition is automatically detected by CMake during compilation.
